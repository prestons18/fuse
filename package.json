{
  "name": "@prestonarnold/fuse",
  "version": "0.1.4",
  "description": "A lightweight JSX framework that uses fine-grained reactivity, similar to Solid.",
  "main": "dist/fuse/index.js",
  "types": "dist/types/fuse/index.d.ts",
  "scripts": {
    "dev": "npm run build -- --watch",
    "build": "tsc && node build.mjs",
    "build:production": "tsc && node build.mjs --prod",
    "type-check": "tsc --noEmit",
    "test": "echo \"np compliance\""
  },
  "keywords": [],
  "author": "Preston Arnold",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "latest",
    "typescript": "latest"
  },
  "files": [
    "dist"
  ]
}
